package com.dimqa.constants;

import com.dimqa.model.User;

public class Users {
    public static final User REGISTERED = new User(
            "registered", "already@registered.ru","already"
    );
}
